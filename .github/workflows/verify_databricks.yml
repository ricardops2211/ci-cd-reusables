name: verify_databricks

on:
  workflow_call:
    inputs:
      databricks_host:
        description: "URL del workspace de Databricks"
        required: true
        type: string
    secrets:
      DATABRICKS_TOKEN:
        required: true

jobs:
  verify:
    runs-on: self-hosted
    env:
      DATABRICKS_HOST: ${{ inputs.databricks_host }}
      DATABRICKS_TOKEN: ${{ secrets.DATABRICKS_TOKEN }}
    steps:
      # 🔹 3) Sanity check de conexión
      - name: Validar conexión a Databricks
        shell: bash
        env:
          PATH: ${{ env.PATH }}:~/databricks-env/bin
        run: |
          source ~/databricks-env/bin/activate
          set -Eeuo pipefail
          databricks --version
          # Lista raíz del workspace para verificar credenciales
          databricks workspace ls / >/dev/null
